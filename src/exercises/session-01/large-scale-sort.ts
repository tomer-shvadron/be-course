/*
 * Large scale sort
 * Given an input text file named `input.txt` that contains 5M lines
 * Each line contains up to 10K characters (a-z)
 * Create an output file named `output.txt` that contains the input sorted
 * Limitations - 1GB of RAM, 1 CPU core, 500GB of disk space
 * To validate - use `sort -c output.txt`
 */

export const largeScaleSort = async () => {
  // Read the input file line by line
  // Create the output.txt file to store the sorted lines
  // Push the line into it's correct spot in the output file
  //
};
